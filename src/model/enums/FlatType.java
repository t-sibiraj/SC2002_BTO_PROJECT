package enums;

/**
 * The {@code FlatType} enum defines the types of flats available in the BTO system.
 */
public enum FlatType {

    /** A 2-room flat type. */
    TWOROOM,

    /** A 3-room flat type. */
    THREEROOM
}